// Flutter requirements
import 'package:flutter/material.dart';

class DipsenGraphComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.album,
      size: 200.0
    );
  }
}